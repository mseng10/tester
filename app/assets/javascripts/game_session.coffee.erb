update = (data) ->
  location.reload()
  return

$ ->
  pusher = new Pusher('<%= Pusher.key %>')
  channel = pusher.subscribe('update_'+window.location.href.replace(/.+\/.+\//i, ''))
  channel.bind 'up', (data) ->
    update data
    return
  return