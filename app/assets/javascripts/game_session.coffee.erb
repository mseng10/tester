update = (data) ->
  location.reload()
  return

$ ->
  pusher = new Pusher('<%= Pusher.key %>')
  channel = pusher.subscribe('update')
  channel.bind 'up', (data) ->
    update data
    return
  return